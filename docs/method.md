# Пригодность архива космопогоды для строгого replay

Этот документ фиксирует исследовательскую часть задачи **FN-23 (S1-05)**:
доказательство того, что хотя бы один архив продуктов/сообщений
космической погоды пригоден для строгого `historical_forecast`
(.ai/main-prompt.md §1, §11) — с проверяемым временем выпуска до `as_of` и
задокументированным охватом/пробелами за обязательный период
01.05–30.06.2024. Это **доказательство доступности**, не реализованный
replay всего сервиса: полная загрузка архива в хранилище — задача этапа 3
(§5 ниже).

Реестр источников — [`sources.yaml`](../sources.yaml) (записи
`nasa-donki-notifications` и `noaa-swpc-forecast-discussion-archive` в
разделе `space_weather:`). Код — [`src/sources/archive_probe.py`](../src/sources/archive_probe.py).
Реальные сохранённые ответы, на которых проверены факты ниже, — байт-в-байт
в [`tests/fixtures/sources/archive/`](../tests/fixtures/sources/archive/README.md)
вместе с `*.meta.json` (URL, `fetched_at`, заголовки ответа, SHA-256).
Тесты, перепроверяющие эти факты заново на каждом прогоне, —
[`tests/sources/test_archive_publication.py`](../tests/sources/test_archive_publication.py).

## Оглавление

1. [Кандидаты и выбор](#1-кандидаты-и-выбор)
2. [Почему это строгий replay, а не правдоподобная реконструкция](#2-почему-это-строгий-replay-а-не-правдоподобная-реконструкция)
3. [Карта наличия/пробелов](#3-карта-наличияпробелов)
4. [Готовые случаи для тестов приёмки](#4-готовые-случаи-для-тестов-приёмки)
5. [Способ загрузки в этапе 3](#5-способ-загрузки-в-этапе-3)
6. [Ограничения и блокеры](#6-ограничения-и-блокеры)
7. [NOAA 3-Day Forecast S1+ — отдельная линия внешнего прогноза (FN-31, S2-01)](#7-noaa-3-day-forecast-s1--отдельная-линия-внешнего-прогноза-fn-31-s2-01)
   - [7.4 Подключение к расчётному API](#74-подключение-к-расчётному-api-round-3-ревью-pr-24)
   - [7.5 Известный, явно не скрытый пробел приёмки: реальные фикстуры](#75-известный-явно-не-скрытый-пробел-приёмки-реальные-фикстуры)
8. [Наблюдаемый GOES S-классификатор — Механизм 1 (FN-38, S2-08)](#8-наблюдаемый-goes-s-классификатор--механизм-1-fn-38-s2-08)
9. [Адаптер строгой выборки архивной космопогоды (FN-42) — публичный контракт](#9-адаптер-строгой-выборки-архивной-космопогоды-fn-42--публичный-контракт)
10. [Стенд экспериментов Т5 (FN-43)](#10-стенд-экспериментов-т5-fn-43)

## 1. Кандидаты и выбор

main-prompt.md §11 называет два кандидата с явным временем выпуска:
сообщения NASA CCMC DONKI и текстовые сводки NOAA SWPC с отметкой `Issued`
в архиве NOAA NCEI. Оба проверены на реальных ответах серверов, а не по
документации:

| Источник | Тип продукта | Проверенный охват периода | Время публикации | Статус |
| --- | --- | --- | --- | --- |
| **NASA CCMC DONKI** (`/DONKI/notifications`) | Событийный (уведомление при пересечении порога) | 01.05–30.06.2024, 245 уведомлений, без молчания длиннее недели | `messageIssueTime`, сверенный с телом (§2) | **Основная линия строгого replay** |
| **NOAA SWPC Forecast Discussion** (архив NCEI) | Периодический (дважды в сутки) | 1–14 мая и 17–30 июня 2024 (56 из 122 слотов); **пробел 15.05–16.06.2024 — выпусков нет** | Строка `:Issued:` в теле бюллетеня | Дополнительная линия, с явным пробелом |

**Выбор: DONKI — основной источник строгого прогноза из прошлого по
Механизму 1.** У него нет сопоставимого многонедельного молчания внутри
обязательного периода (наибольший непрерывный провал — 2 дня, §3), и он
единственный даёт хоть какие-то проверяемо-датированные уведомления во
время пробела SWPC. SWPC Forecast Discussion регистрируется как
независимая вторая линия (`sources.yaml`, `independence_note`) с честно
зафиксированным пробелом — не отбрасывается, потому что покрывает
выраженное событие 10–11 мая собственным, отдельным от DONKI, поставщиком
и форматом.

**Важная оговорка (round 1 ревью PR #18), чтобы не переоценить, что доказано.**
DONKI — событийный продукт: уведомление появляется, когда конкретный
показатель пересёк порог, а не на регулярной сетке времени. Присутствие
уведомления в день `X` доказывает «что-то было опубликовано в этот день»,
но отсутствие уведомления **не доказывает** ни «состояние было спокойным»,
ни «был доступен непрерывный прогноз на 6 часов вперёд» — эти две вещи
доказывает только периодический продукт с явным горизонтом (каким SWPC
Forecast Discussion является структурно, но не покрывает 15.05–16.06.2024
физически). Поэтому DONKI **не «компенсирует»** пробел SWPC как замену
периодического прогноза — он даёт независимую эпизодическую линию
предупреждений о конкретных событиях на то же время, что и является
самостоятельной, отдельно ценной формой строгого replay по Механизму 1,
но не тем же самым обещанием, что и непрерывный прогноз.

Обе линии — **космическая погода**, не MMOD: MMOD не является заменой
строгого прогноза из прошлого по этой линии (main-prompt.md §11, правка
после `chore(.ai): §11` от 2026-09-19 — запасной вариант «строгий прогноз
по MMOD» убран из постановки).

## 2. Почему это строгий replay, а не правдоподобная реконструкция

Правило §1 main-prompt.md: в `historical_forecast` попадают только записи с
`published_at <= as_of`. Обе линии дают **машиночитаемое, проверяемое**
время публикации — не выведенное из времени измерения события и не из
времени, когда эта задача сделала запрос:

- **DONKI**: `messageIssueTime` — задокументированное поле каждого
  уведомления верхнего уровня, отдельное от времени HTTP-запроса, **сверенное
  с независимым вторым указанием времени внутри самого содержания** —
  строкой `## Message Issue Date:` в `messageBody` (round 1 ревью PR #18:
  доверять одному полю без сверки небезопасно). На 245 реальных
  уведомлениях 244 согласуются в пределах округления верхнего поля до
  минуты; для одного (`20240516-7D-001`) расхождение — **~14 часов**
  (верхнее поле `03:44Z`, тело `17:40:05Z`) — в этом случае
  `published_at` становится `None`, а не одним из двух правдоподобных, но
  непроверяемых значений (`_resolve_donki_issue_time`,
  `src/sources/archive_probe.py`).
- **SWPC Forecast Discussion**: `:Issued:` — строка внутри тела, отдельная
  от имени файла/слота запроса. Расхождение подтверждено на реальном
  ответе: файл `202405100030forecast_discussion.txt` (слот «00:30») по
  факту выпущен в **00:35 UTC** — пять минут позже номинального слота.
  Независимо подтверждено HTTP-заголовком `Last-Modified`, который на всех
  проверенных ответах совпадает с `:Issued:` (`*.meta.json` рядом с каждым
  файлом). Коннектор, доверяющий имени файла или времени запроса вместо
  `:Issued:`, тихо занизил бы или завысил `published_at` — на границе
  часа/минуты это была бы реальная утечка или излишняя строгость.

Обе величины вычисляются `src/sources/archive_probe.py` и передаются в уже
существующее и уже протестированное правило хранилища
(`src/store/records.py::select_as_of`, `tests/store/test_as_of.py`) без
изменений в самом правиле — зонд поставляет корректно нормализованные
записи, не переизобретает отсечение.

## 3. Карта наличия/пробелов

Построена `src/sources/archive_probe.py::build_coverage_report`,
перепроверяется тестами `tests/sources/test_archive_publication.py`.

### NASA CCMC DONKI (245 уведомлений, 4 окна по 15 дней)

Непрерывные ряды дней без единого уведомления внутри 01.05–30.06.2024:

| С | По | Длина |
| --- | --- | --- |
| 2024-05-19 | 2024-05-19 | 1 день |
| 2024-05-26 | 2024-05-26 | 1 день |
| 2024-06-03 | 2024-06-04 | 2 дня |
| 2024-06-16 | 2024-06-16 | 1 день |
| 2024-06-21 | 2024-06-21 | 1 день |
| 2024-06-27 | 2024-06-27 | 1 день |
| 2024-06-29 | 2024-06-29 | 1 день |

Наибольший провал — 2 дня. Это ожидаемо (не каждый день есть событие
космопогоды, достойное уведомления) и не сопоставимо с провалом SWPC ниже.
53 из 61 дня периода содержат хотя бы одно уведомление.

**Что это доказывает, а что нет (round 1 ревью PR #18).** Ненулевой день —
это «в этот день что-то опубликовано», не «на этот день есть непрерывный
6-часовой прогноз» (DONKI — событийный продукт, §1). Нулевой день — это «в
этот день порог не был пересечён», не подтверждение «состояние было
спокойным»: main-prompt.md §2 запрещает именно такую подмену
(«отсутствие сигнала» ≠ «подтверждённо благоприятная обстановка»).
`build_coverage_report` документирует это различие явно в своём docstring.

### NOAA SWPC Forecast Discussion (архив NCEI)

Проверено на **полном перечне файлов каталога** — HTML-листинги
`swpc_forecast_discussion_listing_2024-05.html` и `…-06.html`
(`src/sources/archive_probe.py::parse_swpc_forecast_discussion_listing`),
не только на 11 выборочно скачанных телах бюллетеней:

- Май: 28 файлов, `2024-05-01` … `2024-05-14` (дважды в сутки, без пропуска
  внутри диапазона).
- Июнь: 28 файлов, `2024-06-17` … `2024-06-30` (дважды в сутки, без
  пропуска внутри диапазона).
- **Единственный, но полный пробел: `2024-05-15` … `2024-06-16` (33 дня,
  66 слотов) — в каталоге нет ни одного файла.** Подтверждено тестом
  `test_listing_based_coverage_confirms_the_gap_is_a_real_archive_property`:
  это свойство архива, а не следствие того, что эта задача скачала не все
  тела бюллетеней.

Тела 11 выборочно скачанных бюллетеней (`tests/fixtures/sources/archive/README.md`)
подтверждают точное время `:Issued:` по обе стороны пробела и не участвуют
в определении границ пробела — эту роль играет полный листинг каталога.

### Время события против времени публикации

`observed_at`/`valid_from`/`valid_to` DONKI-записей извлекаются из
структурированных полей тела уведомления, **не** из времени публикации
(round 1 ревью PR #18 — main-prompt.md §1 «четыре времени различаются»):

- `Activity ID: <момент>-<ТИП>-<NNN>` (CME/GST/IPS/MPC/RBE/SEP и часть
  FLR — 191 из 245) → точечное событие, `observed_at = valid_from =
  valid_to` = этот момент.
- `Report Coverage Begin/End Date:` (тип `Report` — еженедельная сводка,
  10 из 245) → интервал, `valid_from`/`valid_to` = границы покрытия.
- Уведомления без ни одного из двух полей (44 из 245 — часть FLR с
  формулировками вроде «Flare M5.0 crossing time: …» без `Activity ID`) —
  `donki_notification_to_record_input` возвращает `None`: честнее не
  произвести запись, чем подставить время публикации заглушкой. Итог: ~82%
  реального архива (201 из 245) превращаются в `RecordInput`, остальные —
  осознанно нет (см. §6, п.1).

Все такие записи несут `quality = "reconstructed"` — время события
извлечено из вспомогательного структурированного поля, не измерено
источником напрямую. То же для SWPC Forecast Discussion:
`swpc_forecast_discussion_to_record_input` больше не подставляет
`valid_to = issued_at + 12 часов` (прежняя версия, опровергнутая реальным
текстом бюллетеней — реальный горизонт 2–3 суток, а не 12 часов); вместо
второго придуманного числа запись честно утверждает только точку (момент
выпуска), не интервал.

### Соотнесение с обязательными датами эксперимента

Формулировка «покрыто» ниже — про DONKI означает «есть опубликованное
уведомление в этот день», не «есть непрерывный прогноз на весь день» (см.
оговорку выше и в §1); про SWPC Forecast Discussion — структурно сильнее,
это периодический продукт.

| Дата/период | Что | DONKI (уведомление есть) | SWPC Forecast Discussion |
| --- | --- | --- | --- |
| 2024-05-10…11 | Выраженное событие (AR3664, X5.8, Kp до 9) | Да (14 уведомлений по одной активности, §4) | Покрыто (6 бюллетеней) |
| 2024-06-08 | Протонное событие S1 | Да (`20240608-AL-005/006`) | **Не покрыто** (внутри пробела) |
| 2024-06-16…27 | Контрольный спокойный период | Нет уведомлений SEP/GST в этом диапазоне (не то же самое, что «подтверждённо спокойно», см. оговорку выше) | Частично (17–27 июня покрыто, 16 июня — последний день пробела) |
| 2024-06-28 | Геомагнитная буря Kp 7.67 (граница контрольного периода) | Да (`20240628-AL-004/005`) | Покрыто |

Отсюда — уточнение контрольного периода до **16–27 июня 2024** в
main-prompt.md §11 (28 июня явно исключено бурей).

## 4. Готовые случаи для тестов приёмки

Обязательный тест на утечку времени (main-prompt.md §9.1) реализован на
реальной паре уведомлений, не на синтетических данных:

- `20240511-AL-009` (04:50Z) — первичное уведомление о CME.
- `20240511-AL-012` (14:23Z) — «Update on CME … (see previous notification
  20240511-AL-009)» — уточнение того же события.

`tests/sources/test_archive_publication.py::test_late_update_notification_is_excluded_by_as_of`
вставляет обе записи в реальное хранилище (`src/store/records.py::insert_record`)
и проверяет `select_as_of(as_of="2024-05-11T12:00Z")`: в выборку попадает
только `AL-009`; `AL-012`, опубликованный после отсечения, исключён. Симметричный
тест `test_earlier_as_of_excludes_both_notifications` проверяет более раннее
отсечение (до 04:50Z) — обе записи отсутствуют.

**Неизвестная публикация исключена — два случая, оба на реальных данных
после round 1 ревью PR #18.**

1. **Отсутствие разбираемого времени вовсе** — на реальных ответах не
   встречается (оба источника документированно всегда несут хоть
   какое-то время выпуска). Покрыт двумя явно синтетическими фикстурами
   (`tests/fixtures/sources/archive/synthetic/`, со своим README, без
   `*.meta.json`): бюллетень без строки `:Issued:` и уведомление DONKI с
   неразбираемым `messageIssueTime`. Оба парсера поднимают
   `ArchiveFormatError`.
2. **Расходящиеся указания времени — реальный случай, не синтетика.**
   `20240516-7D-001` (тип `Report`): `messageIssueTime` = `03:44Z`, тело —
   `## Message Issue Date: 17:40:05Z`, расхождение ~14 часов. Ни одно из
   двух не отбрасывается как заведомо неверное — оба могли бы быть
   правдой, и гадать нельзя (main-prompt.md §1). `published_at` этой
   записи — `None`; `select_as_of` не возвращает её ни при каком `as_of`,
   включая моменты между двумя кандидатами и годы спустя
   (`tests/sources/test_archive_publication.py::test_donki_conflicting_issue_time_fields_are_not_silently_trusted`).
   Запись при этом не отбрасывается целиком — её `observed_at`/`valid_from`/
   `valid_to` (из `Report Coverage Begin/End Date`, надёжно извлекаемых
   независимо от вопроса публикации) сохраняются, просто без права на
   строгий replay.

## 5. Способ загрузки в этапе 3

> **Статус после FN-42:** загрузка реализована — `src/sources/archive_ingest.py`,
> публичный контракт в [§9](#9-адаптер-строгой-выборки-архивной-космопогоды-fn-42--публичный-контракт).
> Раздел ниже сохранён как исходный план этапа 3 и как перечень требований,
> которым эта загрузка обязана соответствовать (пакетные окна, бессрочный
> кеш архива, пропуск `None` вместо заглушки, интерпретация — не здесь).

Эта задача не реализует production-шлюз наподобие
`src/sources/swpc.py::fetch_and_store` (периодический refresh, TTL,
заморозка/отключение) — main-prompt.md §11 прямо ограничивает объём FN-23
доказательством пригодности. Для этапа 3:

1. **Постраничная загрузка, не поштучные запросы.** DONKI: запрашивать
   период 15-дневными окнами (наблюдаемый лимит — 10 запросов/час на ключ,
   `sources.yaml`), не по дате отдельно — иначе прогон по всему
   обязательному периоду потребовал бы десятки запросов вместо четырёх
   (main-prompt.md §6 «стенд экспериментов ходит в архив пакетно»). SWPC:
   загружать по каталогу помесячно, используя листинг
   (`parse_swpc_forecast_discussion_listing`) как список того, что вообще
   стоит запрашивать, а не перебирать все возможные слоты вручную.
2. **Кеш архивных ответов бессрочен** (main-prompt.md §5, `sources.yaml`
   → `archive_cache` каждой записи) — в отличие от TTL-обновления текущего
   потока протонов (FN-22): архивный ответ не может измениться, повторный
   запрос того же слота — не операция.
3. **Нормализация уже готова, но не для всех уведомлений.**
   `donki_notification_to_record_input` возвращает `RecordInput | None` —
   `None` для уведомлений без извлекаемого времени события (§3, ~18%
   реального архива); этап 3 обязан пропускать `None`, не подставлять для
   них заглушку. `swpc_forecast_discussion_to_record_input` всегда
   возвращает готовую запись, но с точечным (не интервальным)
   `valid_from`/`valid_to` — этап 3 либо принимает это ограничение, либо
   реализует извлечение реального горизонта прогноза по темам бюллетеня
   (это уже интерпретация, см. п.4).
4. **Интерпретация — не эта задача и не этап 3 загрузки.** Домен
   (`src/domain/spaceweather`, зона 3) решает, как разные `messageType`
   DONKI превращаются в уровни/пороги, как схлопывать связанные
   уведомления одного события (main-prompt.md §4) в один вклад, и как (при
   необходимости) извлекать время события для оставшихся ~18% уведомлений
   и реальный горизонт прогноза SWPC по темам — зонд только нормализует то,
   что можно извлечь из структурированных полей без разбора свободной
   прозы (§8 «получение не считает физику»).

## 6. Ограничения и блокеры

1. **~18% реальных DONKI-уведомлений (44 из 245) не превращаются в
   `RecordInput` вовсе** — у них нет ни `Activity ID`, ни `Report Coverage
   Begin/End Date`, структурированных полей, из которых этот зонд извлекает
   `observed_at`/`valid_from`/`valid_to` (round 1 ревью PR #18, заменяет
   прежнее решение — подстановку времени публикации заглушкой, что было
   прямым нарушением main-prompt.md §1 «четыре времени различаются»).
   Затронутые — часть уведомлений типа FLR с иными формулировками
   («Flare M5.0 crossing time: …»). Извлечение произвольной формулировки
   под каждый вариант — разбор свободного текста, то есть интерпретация
   содержания, а не получение (main-prompt.md §8), и остаётся зоне 3.
2. **Расхождение `messageIssueTime` и тела письма — реальный, а не
   гипотетический случай.** `20240516-7D-001` расходится на ~14 часов
   (§4). Правило разрешения (`_resolve_donki_issue_time`,
   `src/sources/archive_probe.py`): совпадение в пределах округления до
   минуты — берётся точная версия из тела; расхождение свыше 90 секунд —
   `published_at = None`, запись не гадает, какое поле верно. Насколько
   часто это происходит в остальном архиве DONKI за пределами
   проверенных 245 уведомлений — не установлено этой задачей.
3. **Связанные уведомления одного события не схлопнуты.** Активность
   `2024-05-08T05:36:00-CME-001` упомянута в 14 уведомлениях подряд (CME →
   SEP → IPS → MPC → GST → RBE) — main-prompt.md §4 «связанные сигналы
   одного события дают один вклад» обязана быть реализована выше по
   пайплайну (домен); этот зонд нормализует каждое уведомление как
   отдельную запись, что и корректно на этом слое (§8 «получение не
   считает физику»), но не должно быть принято доменным слоем как «14
   независимых событий» без дедупликации по `Activity ID`.
4. **Архив текстовых оповещений SWPC (alerts/watches/warnings, коды вроде
   `WATA20`/`ALTEF3`) не найден** в проверенном разделе NCEI — не
   зарегистрирован как отдельный источник. Не блокирует задачу: DONKI —
   основная линия, Forecast Discussion — достаточная вторая линия с честно
   зафиксированным пробелом.
5. **Пробел SWPC 15.05–16.06.2024 не восполняется этой задачей и не
   компенсируется DONKI как заменой периодического продукта** (round 1
   ревью PR #18 — прежняя формулировка «компенсируется» была неточной,
   см. §1). Для этого интервала SWPC Forecast Discussion остаётся
   непригодным для строгого replay — это не «спокойный период», а «данных
   нет» (главное различие, main-prompt.md §2). DONKI в этом интервале даёт
   независимую эпизодическую линию (уведомление публикуется — событие
   было; уведомления нет — порог не пересечён, не «подтверждённо
   спокойно»), а не непрерывный прогноз с явным горизонтом.
6. **`swpc_forecast_discussion_to_record_input` больше не утверждает
   интервал действия.** `observed_at = valid_from = valid_to = issued_at`
   (точка) — прежняя версия (`valid_to = issued_at + 12 часов`) была
   опровергнута реальным текстом бюллетеней (горизонт 2–3 суток, не 12
   часов, round 1 ревью PR #18). Реальный горизонт различается по каждой
   из четырёх тем бюллетеня (Solar Activity/Energetic Particle/Solar
   Wind/Geospace) отдельными фразами — извлечение по каждой теме
   отдельно является разбором свободного текста и остаётся зоне 3.

## 7. NOAA 3-Day Forecast S1+ — отдельная линия внешнего прогноза (FN-31, S2-01)

Обязательное изменение №1 (`docs/decision/01_solution.md`/`03_verdict.md`):
конкретная линия внешнего прогноза космопогоды — NOAA SWPC «3-Day
Forecast», раздел B «NOAA Solar Radiation Storm Forecast», строка «S1 or
greater» — суточная вероятность (0–100%) события уровня S1 и выше на
каждый из трёх прогнозных дней. Реализация: коннектор и парсер
[`src/sources/noaa_3day_forecast.py`](../src/sources/noaa_3day_forecast.py),
интерпретация для окна ВКД —
[`src/domain/spaceweather/external_forecast.py`](../src/domain/spaceweather/external_forecast.py),
реестр — `sources.yaml` (`noaa-swpc-3day-forecast`,
`noaa-swpc-3day-forecast-archive`).

### 7.1 Отдельно от наблюдения GOES

`noaa-swpc-proton-flux` (FN-22) — наблюдение, `pfu`, `record_kind=observation`.
Эта линия — внешний прогноз, `percent`, `record_kind=forecast`. Разные
`source_id`, разные единицы, разный модуль интерпретации — main-prompt.md
§4 «наблюдение, внешний прогноз и расчёт команды — разные типы данных»
реализовано структурно (нельзя случайно передать одно вместо другого), не
только соглашением об именовании.

### 7.2 Версионирование даёт строгий `historical_forecast` без отдельного кода

Один и тот же прогнозируемый календарный день появляется в трёх
последовательных бюллетенях подряд (сначала как «Day 3», затем «Day 2»,
затем «Day 1», с уточняемой вероятностью). `provider_record_id` этой линии
идентифицирует **день**, не бюллетень (`noaa-3day-s1-plus:<YYYY-MM-DD>`);
`source_version` — момент выпуска конкретного бюллетеня. Из этого уже
существующее и протестированное правило `src/store/records.py::select_as_of`
(`published_at <= as_of`, наибольшая версия на день) реализует приёмку
FN-31 п.3 («поздний выпуск после `as_of` не меняет строгий результат;
неизвестная публикация исключается») автоматически, без нового кода
отсечения — см. тест
`tests/sources/test_noaa_3day_forecast.py::test_later_bulletin_revision_of_the_same_day_is_excluded_by_as_of`.

### 7.3 Обязательная семантика — где именно она реализована

- **Не делится по часам / не умножается на длительность окна / не
  суммируется через полночь / не называется вероятностью ВКД**:
  `src/domain/spaceweather/external_forecast.py::assess_external_forecast`
  структурно не содержит операции деления или умножения вероятности —
  единственная выполняемая операция — пересечение интервалов дат; тест
  `test_window_spanning_midnight_keeps_both_days_separate_not_summed`
  проверяет, что окно, пересекающее полночь, даёт ДВЕ отдельные записи о
  пересечении, а не одну сумму.
- **Исходные прогнозные дни и их пересечение с окном показываются**:
  `ForecastDayWindowOverlap` несёт `day` (исходный, неизменённый) и
  `overlap_start`/`overlap_end` — границы пересечения, не производную
  величину.
- **Длительность фактического превышения из суточной вероятности
  неизвестна**: `ExternalForecastAssessment` не содержит поля вида
  `exceedance_hours` — сознательное отсутствие, не пропуск.
- **Частичное покрытие окна не маскируется под полную оценку** (round 1
  ревью PR #24): `critical_gap` — не «ни один день не пересёкся», а
  «суммарная длительность пересечений меньше длительности всего окна».
  Без этой поправки окно 22:00-04:00 с записью только за первый день
  выглядело бы как полностью оценённое (`critical_gap=False`) с
  правдоподобным `max_probability_percent`, хотя период после полуночи
  вообще не покрыт прогнозом — main-prompt.md §2 «отсутствие данных не
  подменяется благоприятной оценкой» распространяется и на «частично»,
  неотличимое снаружи от «полностью». `coverage_fraction` — доля окна,
  покрытая хотя бы одним прогнозным днём, для дальнейшего использования
  правилом предпочтения окон (main-prompt.md §11, п.1 «критический пробел
  по любому механизму»). Дни — разные календарные сутки и не могут
  пересекаться друг с другом, поэтому сумма длительностей отдельных
  пересечений равна длительности их объединения без риска задвоить общий
  отрезок — сравнение длительностей интервалов, а не деление/суммирование
  самой вероятности. Тесты:
  `test_partial_coverage_is_still_a_critical_gap_not_a_full_assessment`,
  `test_full_coverage_across_two_days_is_not_a_critical_gap`.
- **Несколько версий одного календарного дня не задваивают покрытие**
  (round 2 ревью PR #24): предположение «дни не пересекаются друг с
  другом» из предыдущего пункта верно только для РАЗНЫХ календарных дней
  — если на вход пришло несколько записей за один и тот же
  `forecast_day` (обе линии — живая и архивная — отдали версию на одну
  дату, или вызывающая сторона не отфильтровала устаревшую версию до
  вызова), их пересечения с окном идентичны, и наивое суммирование по
  каждой записи задвоило/затроило бы покрытие: окно 22:00-04:00 с тремя
  версиями первого дня ложно показало бы `coverage_fraction=1.0` вместо
  настоящих `2/6`, хотя второй день вообще не покрыт. `assess_external_forecast`
  теперь предварительно выбирает ровно одну запись на `forecast_day` —
  с наибольшим `published_at` (тот же принцип «самая свежая известная
  версия», что и в `select_as_of`) — до расчёта пересечений; после этого
  дедуп-шага сумма длительностей гарантированно не превышает длительность
  окна (`assert`, не клампинг постфактум, как было в round 1). Тест:
  `test_multiple_versions_of_the_same_day_do_not_inflate_coverage`.
- **Issued/published_at, valid_from/valid_to, fetched_at и observed_at не
  подменяются**: `noaa_3day_forecast_to_record_inputs` — четыре поля
  времени заполняются из четырёх разных источников (заголовок бюллетеня →
  `published_at`; колонка таблицы → `valid_from`/`valid_to`/`observed_at`;
  параметр вызова → `fetched_at`); тест
  `test_to_record_inputs_produce_one_record_per_day_with_full_day_interval`
  проверяет их попарное различие.
- **Документированный переключатель источника действует и для архивной
  линии** (main-prompt.md §5, §10; round 1 ревью PR #24): до этой правки
  `fetch_and_store_archived_bulletin` сразу шёл в сеть, не проверяя
  `enabled`/заморозку/активную квотную паузу после `429` — в отличие от
  `fetch_and_store` для живого продукта. Теперь обе функции проверяют одни
  и те же три состояния (`enabled` из `sources.yaml`, заморозка реестра,
  `quota_cooldown_until`) через общий `Noaa3DayForecastSourceConfig`;
  единственное отличие архивной линии — отсутствие TTL-проверки (архивный
  ответ не устаревает, main-prompt.md §5 «кеш архивных ответов бессрочен»).
  Тесты:
  `test_fetch_and_store_archived_bulletin_disabled_source_never_touches_network`,
  `test_fetch_and_store_archived_bulletin_frozen_never_touches_network`,
  `test_fetch_and_store_archived_bulletin_respects_quota_cooldown`.

### 7.4 Подключение к расчётному API (round 3 ревью PR #24)

Раунды 1-2 ревью касались корректности самого домена
(`external_forecast.py`) и надёжности коннектора; раунд 3 указал на
отдельную проблему — ни одна из этих функций не вызывалась из
`src/api/service.py`, то есть суточная вероятность получалась и
сохранялась, но нигде не участвовала в расчёте, `/sources/refresh`,
`/sources/status`, окнах или `data_manifest`. Реализовано:

- `_build_current_result` вызывает `noaa_3day_source.fetch_and_store` тем
  же управляемым способом, что и `swpc_source.fetch_and_store` (отказ не
  роняет расчёт целиком, main-prompt.md §5), и добавляет
  `noaa-swpc-3day-forecast` в `source_status` каждого результата.
- `refresh_sources`/`get_all_source_status` делают то же самое для
  `/api/sources/refresh`/`/api/sources/status` — документированный
  переключатель `enabled`/заморозка виден и здесь, тем же способом, что и
  для потока протонов.
- Для каждого окна `select_as_of(as_of=now, source_id=noaa-swpc-3day-forecast,
  record_kind="forecast")` + `assess_external_forecast` дают исходные
  прогнозные дни и их пересечение с ЭТИМ конкретным окном.
  `select_as_of(as_of=now, ...)` — не отдельный путь для «текущего»
  режима: та же строго протестированная функция строгого replay
  (`src/store/records.py`), потому что `published_at` этой линии всегда
  известен (парсер требует `:Issued:`) — «текущий» результат на момент
  `now` и есть частный случай строгого replay с отсечением в момент
  расчёта.
- Результат — `mechanisms[*].notes`/`record_ids` окна (для `space_weather`)
  и `result.data_manifest` — несёт реальные данные, когда прогноз
  пересекается с окном, а `status` остаётся `not_implemented`:
  contracts/result.schema.json не ограничивает `notes`/`record_ids`
  статусом (только `max_level`/`exceedance_hours_by_level` обязаны быть
  `null` при `status != "ok"`), поэтому реальная, уже полученная
  информация доходит до клиента раньше, чем появится полноценная
  пороговая логика, комбинирующая её с наблюдением GOES.
- `tests/api/conftest.py`: `app_client` теперь подменяет и
  `noaa_3day_source.fetch` (синтетической фикстурой) — без этого любой
  тест через `app_client` реально обращался бы к `services.swpc.noaa.gov`
  (найдено при реализации этой интеграции: тесты стали занимать на два
  порядка больше времени и часть из них не укладывалась в таймаут ожидания
  фонового задания).
- Тест сквозного пути (не только «получено», но и «использовано») —
  `tests/api/test_requests.py::test_noaa_3day_forecast_is_used_in_windows_and_manifest_when_it_overlaps`.

**Что осознанно НЕ сделано и почему.** `mode ∈ {historical_analysis,
historical_forecast}` по-прежнему завершается `historical_mode_not_implemented`
до какой-либо интерпретации механизмов — это ограничение отдельного,
несвязанного коннектора (`src/sources/orbit.py`: Space-Track `GP_HISTORY`
не реализован, main-prompt.md §11 «Траектория»), а не этой задачи; архивная
линия NOAA 3-Day (`noaa-swpc-3day-forecast-archive`) поэтому не может быть
продемонстрирована через `run_calculation` — путь обрывается раньше, на
проверке режима, безотносительно к тому, что уже реализовано в этой линии.
`windows[*].mechanisms[space_weather].critical_gap`/`coverage_fraction` не
переопределяются данными этой линии (остаются `True`/`0.0`, как и раньше)
— эти поля контрактно описывают полноту данных ДЛЯ ГОТОВОЙ оценки
механизма в целом (наблюдение + прогноз главной формулой §11), а не
полноту одной лишь линии внешнего прогноза; наблюдение GOES pfu всё ещё
не входит ни в один расчёт уровня, так что полнота механизма в целом
остаётся неполной независимо от того, насколько полно прогноз покрывает
окно.

### 7.5 Известный, явно не скрытый пробел приёмки: реальные фикстуры

**Приёмка FN-31 п.1 («парсер проверен на сохранённых реальных текущем и
историческом выпусках, включая 2024-05-10 12:30 UTC») и п.2 («карта
доступности/пробелов мая-июня 2024 по телам выпусков») этой сессией не
закрыты.**

В отличие от FN-23 (архивные фикстуры DONKI/Forecast Discussion получены,
как явно указано в `tests/fixtures/sources/archive/README.md`, «из
окружения с прямым доступом к сети» — то есть не из этой песочницы), в
сессии, где готовился PR FN-31, исходящий сетевой прокси отвечает `403`
policy denial на каждый проверенный хост NOAA/NASA
(`services.swpc.noaa.gov`, `www.ngdc.noaa.gov`, `www.swpc.noaa.gov`,
`api.nasa.gov`) — подтверждено прямой попыткой в начале работы над этой
задачей. Это тот же класс ограничения, что уже задокументирован в
корневом `README.md` («Известное ограничение окружения сборки», FN-28),
но здесь он не позволяет получить исходные данные вовсе, а не только
проверить развёрнутый стек.

Вместо того чтобы выдать придуманные числа за «сохранённые реальные
выпуски» (что напрямую противоречило бы главному приоритету проекта —
временной честности, `.ai/main-prompt.md` §1–2), в этой сессии:

- парсер и коннектор реализованы по полностью документированному,
  стабильному публичному текстовому формату продукта (тот же заголовочный
  стиль `:Product:`/`:Issued:`, что и у уже подтверждённого
  `noaa-swpc-forecast-discussion-archive`);
- тесты используют явно синтетические фикстуры
  (`tests/fixtures/sources/noaa_3day_forecast/synthetic/`, свой README,
  без `*.meta.json` — по тому же принципу, что и
  `tests/fixtures/sources/archive/synthetic/`) с датами, намеренно не
  пересекающимися с обязательным периодом 01.05–30.06.2024, чтобы
  исключить любую двусмысленность;
- `sources.yaml` прямо помечает оба новых источника как «НЕ ПОДТВЕРЖДЕНО
  на реальном ответе в этой сессии» (`access_restrictions`,
  `replay_eligible_note`) — реестр не расходится с тем, что реально
  проверено (main-prompt.md §7);
- URL архивного эндпоинта (`noaa-swpc-3day-forecast-archive`) смоделирован
  по образцу уже подтверждённого соседнего продукта
  (`noaa-swpc-forecast-discussion-archive`), но точное имя
  подкаталога/файла NCEI для именно этого продукта не подтверждено.

**Оставшаяся работа (см. также описание PR), требует сетевого доступа к
NOAA/NASA:**

1. Получить и сохранить байт-в-байт реальный текущий ответ
   `services.swpc.noaa.gov/text/3-day-forecast.txt` и реальный исторический
   выпуск на 2024-05-10 12:30 UTC (или ближайший реально существующий слот
   этого времени) из архива NCEI; заменить/дополнить синтетические
   фикстуры реальными по тому же принципу, что и
   `tests/fixtures/sources/archive/README.md`.
2. Подтвердить точный путь архива NCEI для этого продукта (сейчас — по
   аналогии с `noaa-swpc-forecast-discussion-archive`, не проверено
   независимо).
3. Построить карту доступности/пробелов мая-июня 2024 по телам реальных
   выпусков (`src/sources/archive_probe.py::build_coverage_report` уже
   достаточно общая для повторного использования с датами `issued_at` этой
   линии — новой функции агрегации не требуется).
4. Проверить эту карту тестом на реальных данных, аналогично
   `tests/sources/test_archive_publication.py`.

## 8. Наблюдаемый GOES S-классификатор — Механизм 1 (FN-38, S2-08)

Закрывает production-разрыв, выявленный при подготовке FN-37 (см. её
комментарии в Jira): наблюдение GOES pfu (получено и сохранено ещё FN-22,
`src/sources/swpc.py`) до этой задачи получалось и сохранялось, но не
участвовало ни в одном расчёте уровня — `mechanisms[*space_weather].status`
был всегда `not_implemented`. Реализация — чистый доменный модуль
[`src/domain/spaceweather/observed_classifier.py`](../src/domain/spaceweather/observed_classifier.py),
подключение — `src/api/service.py` (`_space_weather_status`,
`_space_weather_mechanism`, `_window_observed_mechanism` внутри
`_build_current_result`).

### 8.1 Пороги — не подобраны, происхождение прослеживается

Уровни S1/S2/S3 взяты дословно из таблицы `.ai/main-prompt.md` §11
«Механизм 1. Радиационная обстановка» (шкала S NOAA: фон <10, S1>=10,
S2>=100, S3>=1000 pfu) — та же таблица, что заранее зарезервирована в
`contracts/result.schema.json` → `mechanismAssessment.exceedance_hours_by_level`
для `mechanism == "space_weather"` (`{S1, S2, S3}`). Ни одно число в
`src/domain/spaceweather/observed_classifier.py` не подобрано этой задачей;
`sources.yaml` → `space_weather[0].classification` дублирует происхождение
рядом с самим источником величины (main-prompt.md §7 «реестр источников —
единственный источник истины»).

### 8.2 Наблюдение — не прогноз: `hold_seconds` и `beyond_horizon`

GOES pfu — мгновенный отсчёт, у него нет собственного горизонта прогноза
вперёд. `assess_observed_flux` НЕ экстраполирует последний отсчёт на весь
остаток окна — main-prompt.md §11 прямо называет «сохранение последнего
наблюдения на весь горизонт» наивным БАЗОВЫМ методом для сравнения, не тем,
что делает настоящая реализация. Вместо этого один отсчёт считается
представительным вперёд не дольше `hold_seconds` (= `sources.yaml` →
`space_weather[0].freshness.ttl_seconds`, тот же интервал, что уже
документирован провайдером как ожидаемый шаг между отсчётами) или до
следующего реального отсчёта, смотря что раньше — и никогда не дальше
текущего момента расчёта (`now`). Часть окна после `now` — `beyond_horizon`
(«не покрыто», main-prompt.md §4), не «спокойно»: так как окна ВКД в
`mode=current` обычно лежат в будущем относительно момента расчёта (аналитик
планирует предстоящий выход), это — типичный, а не крайний случай, и он
прямо проверен интеграционным тестом
(`tests/api/test_observed_flux_integration.py::test_window_entirely_in_the_future_yields_beyond_horizon_status`).

Обоснованный внешний прогноз минимум на 6 часов вперёд для Механизма 1
(main-prompt.md §11) даёт ОТДЕЛЬНАЯ линия — NOAA 3-Day Forecast (FN-31, §7
выше): её суточная вероятность S1+ остаётся в `notes`/`record_ids` того же
`mechanismAssessment` как контекст, но по-прежнему НЕ создаёт собственного
уровня/`exceedance_hours_by_level` — FN-38 не меняет это решение FN-31, две
линии одного механизма остаются разными величинами (main-prompt.md §4).

### 8.3 `status` — кто отвечает за что

`assess_observed_flux` (чистый расчёт, без сети и без реестра источников)
отвечает только за `coverage_fraction`/`critical_gap`/`max_level`/
`exceedance_hours_by_level`/`beyond_horizon`. Финальный код
`mechanismAssessment.status` выбирает `src/api/service.py::_space_weather_status`
— единственное место, которому известны и результат чистого расчёта, и
состояние получения (main-prompt.md §8 «получение не считает физику», здесь
— зеркально: расчёт не знает про сеть/реестр):

| Условие | `status` |
| --- | --- |
| нет пробела вовсе | `ok` |
| окно (хотя бы частично) после `now` | `beyond_horizon` |
| нет пробела, но ИМЕННО эта попытка получения только что отказала (таймаут/квота/HTTP/неожиданная ошибка) | `source_error` |
| источник давно не отвечал успехом (устарел относительно `critical_staleness_seconds`) | `stale_data` |
| источник исправен и свеж, но для интервала окна пригодных отсчётов нет | `missing_data` |

Все пять исходов проверены `tests/domain/spaceweather/test_observed_classifier.py`
(чистая классификация) и `tests/api/test_observed_flux_integration.py`
(через `run_calculation` — ту же функцию, что вызывает HTTP-роутер, не
только доменный unit-тест, приёмка FN-38 п.2).

**Два случая, когда классификация не строится вообще** (round 1 ревью PR #29),
дают `source_error` напрямую, минуя таблицу выше — `_space_weather_error_mechanism`:

- конфигурация источника (`sources.yaml`) недоступна для этой попытки
  расчёта — `hold_seconds`/`critical_staleness_seconds` неизвестны, поэтому
  честный отказ вместо расчёта на запасных/угаданных числах (main-prompt.md
  §7);
- выборка/нормализация сохранённых наблюдений завершилась ошибкой —
  повреждённая запись или неразрешимый конфликт версий
  (`ConflictingObservationsError`, см. 8.3.1 ниже). Раньше такая ошибка
  тихо схлопывалась в обычный `missing_data`, теряя признак того, что
  причина — не «данных нет», а отказ обработки; сейчас, помимо
  `mechanismAssessment.notes`, заводится отдельный `warnings[*]` с
  `window_id` и `record_ids` конкретных затронутых записей (round 2 ревью
  PR #29), и эти же записи попадают в `data_manifest` — id конфликтующих
  наблюдений остаются прослеживаемы (О4), а не видны только в логе сервиса.
  Само предупреждение ссылается (`warning.fetch_attempt_id`) на СВОЙ
  идентификатор попытки выборки/обработки для этого окна — не на
  `swpc_attempt_id` сетевого получения потока (round 3 ревью PR #29: тот
  мог к этому моменту уже успешно завершиться, указывая на постороннее
  событие); идентификатор логируется вместе с `window_id` в ту же запись
  структурного лога (`swpc_observation_selection_failed`/
  `swpc_observation_processing_failed`), доказуемо восстанавливаясь оттуда
  — тот же принцип, что и `_fetch_attempt_id` для сетевых источников
  (round 1 ревью PR #19); это верно для ВСЕХ трёх точек отказа этой функции
  (ошибка самой выборки, `ConflictingObservationsError`, любое другое
  непредвиденное исключение при разборе/расчёте) — round 4 ревью PR #29
  отметил, что ветка `ConflictingObservationsError` изначально строила
  `warning.fetch_attempt_id`, не логируя для него событие; исправлено тем
  же вызовом `_log`, что и в соседней ветке.

#### 8.3.1 Версионирование одной записи против конфликта разных записей

`select_observed_range` (в отличие от `select_as_of`) возвращает ВСЕ версии
одного `provider_record_id` в диапазоне — у этого источника нет
`published_at`, чтобы отсечь их на уровне SQL. `assess_observed_flux`
поэтому сначала выбирает для каждого `provider_record_id` последнюю по
`fetched_at` версию (источник документирует возможность пересчёта отсчёта,
`sources.yaml` → `space_weather[0].quality_notes`) — иначе позднее
исправление ошибочного значения (например `1000` → `5` pfu) не заменяло бы
старую версию, и расчёт продолжал бы показывать ложный уровень. Если у
НЕСКОЛЬКИХ версий одной записи совпадает максимальный `fetched_at` (round 2
ревью PR #29 — раньше в этом случае молча выбиралась первая по порядку
выборки, зависящая от случайного `record_id`, а не от содержимого), но их
содержимое РАЗНОЕ — это тоже `ConflictingObservationsError`, не угадывание
по порядку; полностью одинаковое содержимое конфликтом не считается (выбор
между двумя идентичными версиями не имеет значения). Только ПОСЛЕ шага
версионирования проверяется коллизия по `observed_at` между разными
`provider_record_id` (например два разных спутника на один момент —
источник документирован как единственный «первичный» спутник,
`space_weather[0].independence_note`, штатно такого не бывает): здесь нет
задокументированного правила выбора, поэтому `ConflictingObservationsError`
делает эту неоднозначность видимой явным отказом, а не эвристикой вроде
«взять большее значение» (main-prompt.md §2). Оба случая несут
`record_ids` конкретных конфликтующих записей (не всей выборки окна) —
`src/api/service.py` показывает именно их в `warnings`/`data_manifest`, не
маскирует как обычное непредвиденное исключение (сообщение построено этим
же модулем из `provider_record_id`/`fetched_at`, не из сырого ответа
источника).

### 8.4 Что осталось честно не реализовано

- Геомагнитный модулятор (Kp>=5 расширяет применимость уровня, §11) и
  контекст шкалы R — в кодовой базе нет источника Kp/R, поэтому не
  подключены; это отдельная задача, не скрытая замена нулём.
  `limitations` результата явно об этом говорит.
- Механизм 2 (MMOD) реализован отдельной задачей FN-39, слитой в эту же
  ветку после FN-38: нормировка к спорадическому фону закрыта первичным
  источником NASA MEO 2024 LEO forecast (`ratio_to_background`, пороги
  1.2/2, `src/domain/mmod/background.py`) — подробности, единицы и границу
  интеграции с геометрией станции (FN-32) см. `docs/mechanisms.md` §12, не
  здесь (этот файл — метод Механизма 1, FN-22/FN-38).
- С обоими обязательными механизмами реальными `recommendation.status` для
  `mode=current` больше не гарантированно `all_windows_excluded`: когда оба
  покрыты пригодными данными без критического пробела в конкретном окне,
  правило предпочтения окон (main-prompt.md §11, пп.1-4) реально сравнивает
  окна — `selected`/`tie`/`insufficient_basis`, либо честный
  `all_windows_excluded`, когда хотя бы один механизм не покрыт для
  запрошенных дат. Это по-прежнему не захардкожено — тот же общий вывод
  правила доминирования v2 (`src/domain/windows/dominance.py`), без правок
  `_apply_window_dominance`, см. `docs/mechanisms.md` §12.5 и
  `tests/api/test_observed_flux_integration.py::test_seeded_full_hour_of_observations_yields_ok_status_via_production_path`.

## 9. Адаптер строгой выборки архивной космопогоды (FN-42) — публичный контракт

Раздел §5 выше описывал загрузку архива в хранилище как «задачу этапа 3».
FN-42 её закрывает: `src/sources/archive_probe.py` остаётся **зондом**
(парсеры + нормализация + карта покрытия), а поверх него появился
provider-agnostic адаптер `src/sources/archive_ingest.py`, который
production orchestration потребляет одинаково для любого архивного продукта,
и доменная оценка `src/domain/spaceweather/archive_assessment.py` с тремя
состояниями.

**Что этот раздел сознательно НЕ утверждает.** Вопрос «DONKI или
датированный численный продукт NOAA является допустимой линией строгого
прогноза из прошлого» остаётся на уточнении у кейсодержателя
(main-prompt.md §11). Здесь нигде не сказано, что DONKI уже окончательно
достаточен: продукт и горизонт выбираются конфигурацией, а не кодом, и
оба архивных продукта сегодня объявляют `forecast_horizon_hours: null`.

### 9.1 Контракт для production orchestration

Четыре шага, все — обычные функции без глобального состояния и без сети
внутри домена.

**1. Стратегия — какие продукты и с каким горизонтом.**

```python
from src.sources.archive_ingest import HistoricalArchiveStrategy

strategy = HistoricalArchiveStrategy.from_sources_yaml([
    "nasa-donki-notifications",
    "noaa-swpc-forecast-discussion-archive",
])
```

`HistoricalArchiveStrategy` читает `sources.yaml` и несёт
`ArchiveProductConfig` на каждый продукт: `forecast_horizon_hours`
(`None` = «горизонт не установлен»), `forecast_horizon_note`,
`event_message_types`, `enabled`. Отключённый в реестре продукт
не попадает ни в загрузку, ни в выборку (main-prompt.md §5, критерий Т6).
Ответ эксперта по горизонту закрывается правкой **одного поля реестра** —
`ArchiveProductConfig.with_forecast_horizon_hours(...)` существует для
стенда Т5 и для того, чтобы горизонт оставался данными; ни одна расчётная
функция при этом не меняется.

**2. Загрузка сохранённых архивных ответов в хранилище.**

```python
report = ingest_donki_notifications(
    conn, raw_store, raw_bytes,
    source_url=..., fetched_at=...,
    interval_start=..., interval_end=...,   # реальное окно запроса к архиву
)
```

`ingest_swpc_forecast_discussion(...)` имеет ту же сигнатуру — в этом и смысл
адаптера. Обе возвращают `ArchiveIngestReport`, где категории разделены, а
не свёрнуты в «загружено N»:

| Поле | Что означает |
| --- | --- |
| `stored_record_ids` | записи, реально лежащие в хранилище |
| `replay_eligible_record_ids` | из них пригодные для строгого replay |
| `stored_not_replay_eligible` | сохранены, но `published_at` неизвестен — **навсегда** вне строгого replay |
| `skipped_without_event_time` | запись не создавалась вовсе (время события неизвлекаемо), а не создавалась с заглушкой |
| `skipped_message_types` | `messageType` каждой записи выше, тот же порядок и длина (round 3 ревью PR #37) |
| `conflicts` | тот же дедуп-ключ с ДРУГИМ содержимым — это не дубликат |
| `conflict_message_types` | `messageType` записи, вызвавшей каждый конфликт выше (round 3 ревью PR #37) |
| `interval` | какой интервал архива доказанно прочитан, **с `fetched_at`** — когда именно (round 3 ревью PR #37) |

`interval` — ключевое поле контракта. Отсутствие записей само по себе не
значит ничего; значение имеет только пара «интервал прочитан» + «в нём нет
сообщения о событии». Без него «мы туда не смотрели» было бы неотличимо от
«там ничего не было» (main-prompt.md §2).

Повторная загрузка того же ответа идемпотентна: те же `record_id`, новых
строк нет (дедупликация по `(source_id, provider_record_id, source_version)`
в `insert_record`). Поздние уточнения ложатся **рядом** — хранилище не
предоставляет `UPDATE` вовсе.

**Интервал доверен только пока в нём нет непронормализованного релевантного
уведомления** (round 3 ревью PR #37). `merged_ingested_intervals(reports, *,
source_id, relevant_message_types)` — обязательный (без умолчания) третий
параметр, обычно `policy.event_message_types` того же продукта — склеивает
интервалы **только** тех отчётов, где нет записи в `skipped_message_types`
или `conflict_message_types` из этого множества
(`ArchiveIngestReport.has_unresolved_notifications_of`). Отчёт с
непронормализованным `FLR` не портит доверие для механизма, следящего
только за `SEP`; непронормализованный `SEP` выбрасывает весь интервал этого
отчёта из покрытия целиком — окно внутри него получает `INSUFFICIENT_DATA`
через обычный `critical_gap`, а не тихо остаётся «полностью прочитанным».

**Вход для уже пройденного `as_of` закреплён навсегда одним снимком — записи
И покрытие вместе, за конкретным расчётом** (round 4/6/7 ревью PR #37).
Production orchestration копит и записи, и отчёты о загрузке по мере того,
как архив дозагружается — по любому поводу, не обязательно ради конкретного
`as_of`. Без дополнительной меры вход уже вычисленного в прошлом
`historical_forecast` мог бы задним числом стать полнее и изменить оценку —
то есть результат уже случившегося строгого прогноза из прошлого незаметно
менялся бы от того, что произошло (было загружено) СЕГОДНЯ (main-prompt.md
§3: «сохранённый результат неизменяем»). Поэтому вход для
`assess_archive_window` идёт не напрямую через `select_as_of`/
`merged_ingested_intervals` (докстринг `merged_ingested_intervals` явно
предупреждает об этом), а через единственную задокументированную точку
сборки:

```python
from src.sources.archive_ingest import assemble_historical_forecast_input

inputs = assemble_historical_forecast_input(
    conn, reports, computation_id=computation_id, as_of=as_of, strategy=strategy,
)
# {source_id: HistoricalForecastInput(snapshot_id=..., records=..., ingested_intervals=...)}
```

`computation_id` — обязательный идентификатор КОНКРЕТНОГО расчёта (будущий
`result_id` production orchestration, либо любой другой идентификатор,
который вызывающая сторона генерирует заново для каждого независимого
расчёта). Он существует, чтобы независимые расчёты с одним и тем же `as_of`
не делили закреплённое состояние между собой (round 6 ревью PR #37,
finding 3): без него первый же вызов для пары (`source_id`, `as_of`) — в
том числе случайный или несвязанный с этим конкретным `historical_forecast`
— необратимо решал бы, какой вход увидят ВСЕ последующие независимые
расчёты с тем же `as_of`, включая те, что могли бы честно увидеть более
полный архив. Повторный вызов с ТЕМ ЖЕ `computation_id` — идемпотентный
повтор/ретрай одного и того же расчёта и обязан вернуть тот же
`HistoricalForecastInput`, включая тот же `snapshot_id` —
`tests/sources/test_archive_ingest.py::test_two_independent_computation_ids_do_not_share_a_pinned_snapshot`
проверяет независимость двух расчётов.

Внутри, для каждого продукта стратегии, `assemble_historical_forecast_input`
вычисляет ТЕКУЩИХ кандидатов (`select_as_of` + `merged_ingested_intervals`) и
закрепляет их ОБОИХ ОДНИМ вызовом
`src/store/forecast_snapshot.py::seal_forecast_input_snapshot` (таблицы
`archive_forecast_snapshots`/`..._records`/`..._intervals`, только
`INSERT OR IGNORE`, ключ — `(computation_id, source_id, as_of)`). Более ранняя
редакция (round 4/6) закрепляла только предел `fetched_at` для карты
покрытия — набор ЗАПИСЕЙ при этом выбирался заново при каждом вызове и мог
тихо вырасти между двумя вызовами одного расчёта (round 7 ревью PR #37,
finding 2, 🚨: архив пополнился записью с `published_at <= as_of`, но её
загрузили позже первого вызова —
`tests/sources/test_archive_ingest.py::test_assemble_historical_forecast_input_also_freezes_the_record_set_not_only_coverage`
воспроизводит и закрывает это). Здесь снимок — дословно сохранённое
СОДЕРЖИМОЕ (`record_id` и интервалы), а не скаляр для сравнения: первый
вызов для ключа сохраняет ровно то, что ему передали — **даже пустой вход**
(round 6 ревью PR #37, finding 1 —
`tests/sources/test_archive_ingest.py::test_assemble_historical_forecast_input_freezes_even_when_nothing_has_loaded_yet`),
любой последующий читает сохранённое обратно целиком и ничего не
пересчитывает и не сравнивает заново — структурно неспособен унаследовать
баг «фильтрация после склейки» предыдущих раундов
(`tests/sources/test_archive_ingest.py::test_assemble_historical_forecast_input_stitches_a_load_boundary_and_freezes_it`
проверяет стык двух окон загрузки на реальных фикстурах).

`snapshot_id` — собственный, сервером сгенерированный идентификатор именно
этого снимка (round 7 ревью PR #37, finding 3, ⚠️), не совпадающий и не
обязанный совпадать с `computation_id`. `src/store/forecast_snapshot.py::read_sealed_snapshot(conn, snapshot_id)`
восстанавливает закреплённые `record_id` и интервалы **только** по нему —
без `computation_id`/`source_id`/`as_of` — ровно то, что понадобится, чтобы
сохранить `snapshot_id` в `data_manifest` результата и восстановить
использованный вход из выгрузки, не полагаясь на будущее совпадение
`computation_id` с `result_id` production orchestration
(`tests/sources/test_archive_ingest.py::test_snapshot_id_alone_reconstructs_the_sealed_input_independent_of_computation_id`).

Открыто (не решено этим PR — оркестрация `historical_forecast` не входит в
целевые файлы тикета FN-42): реальное подключение `assemble_historical_forecast_input`
к `src/api/service.py` и выбор `computation_id` там (например, `result_id`
самой production orchestration, которой в этом PR ещё нет). До этого
подключения `assess_archive_window` синтаксически по-прежнему принимает
любые записи/интервалы — контракт этого раздела документирует единственный
ПРАВИЛЬНЫЙ способ их получить, а не запрещает код-level остальные.

**3. Выборка входа прогноза.**

`assemble_historical_forecast_input` выше — рекомендуемый способ получить и
записи, и покрытие вместе. Раздельные строительные блоки остаются публичными
(проверены каждый по отдельности) для случаев, где нужна только одна часть:

```python
selected = select_forecast_inputs(conn, as_of=as_of, strategy=strategy)
# {source_id: [записи contracts/record.schema.json]}
```

Правило не переизобретается: внутри — уже существующий и уже протестированный
`src/store/records.py::select_as_of` (`published_at <= as_of` и
`replay_eligible = true`, `tests/store/test_as_of.py`). Адаптер добавляет
ровно один слой — перебор продуктов, выбранных конфигурацией.

**Последующие наблюдения — другой модуль, а не флаг.**
`src/store/verification.py::select_verification_records` возвращает записи с
`published_at > as_of` для апостериорной проверки качества прогноза. Это
отдельная функция в отдельном модуле: main-prompt.md §1 прямо запрещает
делать вход прогноза и проверку одной функцией с булевым флагом. Условия двух
выборок взаимно исключающи, поэтому наборы не пересекаются
(`tests/store/test_verification_path.py`).

**4. Оценка окна — три состояния.**

```python
forecast_input = inputs[source_id]  # из assemble_historical_forecast_input
assessment = assess_archive_window(
    forecast_input.records,
    policy=ArchiveProductPolicy.from_config(strategy.product_for(source_id)),
    ingested_intervals=[
        CoverageInterval(start=i.start, end=i.end, fetched_at=i.fetched_at)
        for i in forecast_input.ingested_intervals
    ],
    window_start=..., window_end=..., as_of=...,
)
```

Преобразование `IngestedInterval` (`sources/`) в `CoverageInterval` (`domain/`)
остаётся ручным на стороне вызывающего — домен не импортирует `sources/`
(main-prompt.md §8).

`CoverageInterval.fetched_at` — обязательное поле (round 3 ревью PR #37):
когда этот интервал был реально прочитан. Не участвует в отборе по `as_of`
(покрытие устанавливается исследовательским конвейером сегодня для периода
в прошлом — это не то же самое, что `published_at` записи), но выводится в
`notes` для прослеживаемости (критерий О4) и определяет, чьё значение
побеждает при склейке двух перекрывающихся интервалов (более позднее).

| `status` | Когда | Что это значит |
| --- | --- | --- |
| `EVENT_PRESENT` | есть сообщение о событии, пересекающее окно | внешнее предупреждение источника, не расчёт команды и не доза космонавта |
| `NO_EVENT_DETECTED` | интервал прочитан полностью, продукт умеет сообщать о событиях, сообщений нет, и нет неразрешённых начал явлений (ниже) | «порог не пересечён в прочитанном интервале» — **не** «обстановка подтверждена спокойной» |
| `INSUFFICIENT_DATA` | пробел покрытия, за горизонтом, продукт не даёт машиночитаемого признака события, либо в интервале есть начало явления без задокументированного конца | оценить невозможно; никогда не превращается в благоприятную оценку |

**Начало явления без конца — тоже `INSUFFICIENT_DATA`, не `NO_EVENT_DETECTED`**
(round 3 ревью PR #37). `src/sources/archive_probe.py` помечает точечные
записи, извлечённые из `Activity ID` (в т.ч. `SEP`), флагом
`spatial_context["open_ended"] = True` — DONKI не публикует структурированный
момент завершения для этих типов. Окно, лежащее целиком после такой точки
(и всё ещё внутри того же прочитанного интервала архива), не может получить
`NO_EVENT_DETECTED` только из-за отсутствия точного пересечения с точкой
начала — такие записи видны отдельно в
`ArchiveWindowAssessment.unresolved_open_events`, не смешаны с подтверждённо
пересекающими окно `events`.

Дополнительно возвращаются `beyond_horizon`, `critical_gap`,
`coverage_fraction`, `horizon_end`, `record_ids` и `notes` — той же семантики,
что и в `external_forecast.py`/`observed_classifier.py`.

Структурные предохранители: запись с `published_at > as_of` поднимает
`TemporalLeakError` (не игнорируется тихо — это уже случившаяся ошибка
пайплайна), запись с `published_at = null` или чужого продукта —
`UnsupportedRecordError`.

### 9.2 Почему горизонт нигде не зашит числом

`assess_archive_window` берёт горизонт только из политики. Пока
`forecast_horizon_hours` равен `null`, вперёд от `as_of` не засчитывается
никакое покрытие: часть окна после отсечения становится `beyond_horizon` и
даёт `INSUFFICIENT_DATA` — «не покрыто», а не «спокойно» (main-prompt.md §4).
Это не «ноль часов по умолчанию», а отказ засчитывать покрытие там, где его
никто не подтвердил. Ни 6, ни 24 часов в коде нет (приёмка FN-42 п.5);
механизм при этом реализован и протестирован на значениях 3/6/24, задаваемых
тестом через конфигурацию —
`tests/domain/spaceweather/test_archive_assessment.py::test_horizon_end_follows_the_configured_value`.

`event_message_types` живёт там же и по той же причине. Для DONKI это `[SEP]`
— единственный тип, сообщающий именно о протонном событии, то есть об
оцениваемой величине Механизма 1. `GST` сознательно не включён:
геомагнитная активность — модулятор, а не слагаемое (main-prompt.md §11);
`CME`/`FLR`/`IPS`/`MPC`/`RBE` — связанные сигналы того же явления, и их
включение дало бы одному событию многократный вклад (§4). Для NOAA SWPC
Forecast Discussion перечень пуст — продукт представляет собой свободный
текст, и доказанное отсутствие события по нему структурно недостижимо: он
может дать только `INSUFFICIENT_DATA` и потому никогда не выдаёт
непрочитанный текст за подтверждённо спокойную обстановку.

### 9.3 Случаи приёмки — все на реальных сохранённых ответах

| Случай | Данные | Тест |
| --- | --- | --- |
| Событие | `20240510-AL-004` (13:46Z) о `2024-05-10T13:35:00-SEP-001` | `test_real_sep_event_window_is_event_present` |
| Доказанное отсутствие события | контрольный период 16–27.06.2024: в окне архива нет ни одного `SEP` | `test_control_quiet_period_is_no_event_detected_not_insufficient` |
| Пробел | 15.05–16.06.2024 в архиве Forecast Discussion | `test_swpc_ingest_coverage_map_shows_the_real_archive_gap`, `test_window_inside_the_swpc_gap_is_insufficient_data` |
| Дубликат | повторная загрузка того же файла | `test_reingesting_the_same_response_is_idempotent` |
| Позднее уточнение | `20240511-AL-008` (04:13Z) и `20240511-AL-013` (15:03Z) об одной активности `SEP-001` | `test_late_sep_update_is_stored_beside_the_earlier_one_not_over_it` |
| Неизвестная публикация | `20240516-7D-001` (расхождение ~14 ч) | `test_unknown_publication_time_is_stored_but_never_selected` |
| Утечка времени (§9.1) | добавление трёх архивных окон, опубликованных после `as_of` | `test_adding_a_record_published_after_as_of_changes_neither_selection_nor_assessment` |

Обязательный тест на утечку проверяет **и выборку, и оценку**: после
добавления записей, опубликованных позже отсечения, `assessment` не меняется
ни в одном поле (сравнение целых dataclass-ов, а не отдельных полей).

### 9.4 Границы этой задачи

- **Оркестрация не подключена к API.** `src/api/service.py` этой задачей не
  менялся: FN-42 поставляет адаптер и оценку, а включение исторической линии
  в расчётный путь — отдельная задача. Контракт выше описан именно для неё.
- **Связанные уведомления одного явления не схлопнуты.** Ограничение §6 п.3
  остаётся: `event_message_types: [SEP]` снимает многократный вклад
  *разных типов* одного события, но несколько `SEP` об одной активности
  (например `20240510-AL-004` и `AL-005` о `SEP-001`) по-прежнему дают
  несколько записей в `events`. Для `status` это безразлично
  (`EVENT_PRESENT` не считает события), но для будущего подсчёта вклада
  дедупликация по `Activity ID` остаётся невыполненной.
- **Архивная линия NOAA 3-Day Forecast не проверена.** Ключи
  `forecast_horizon_hours`/`event_message_types` ей добавлены, но реальных
  сохранённых ответов этого продукта в репозитории нет (§7.5), поэтому
  загрузка на ней не проверялась.

## 10. Стенд экспериментов Т5 (FN-43)

Закрывает критерий Т5 («сравнение с простым базовым методом на выраженном
событии и контрольном периоде… сохранённые расчёты») — код
[`experiments/`](../experiments/), запуск и форма артефактов описаны в
корневом `README.md` (раздел «Стенд экспериментов Т5»). Этот раздел
фиксирует *почему* выбраны именно эти даты и *что* стенд честно показал на
реальном прогоне `uv run python -m experiments.run` (2026-09-19,
`experiments/out/summary.md`) — не аспирационные формулировки, а прямые
следствия сохранённых артефактов.

### 10.1 Три сценария и происхождение дат

Ни одна дата в [`experiments/scenarios.yaml`](../experiments/scenarios.yaml)
не выбрана произвольно — все они опираются на уже подтверждённую карту §3
выше и на `tests/fixtures/orbit/history/README.md`:

- **event** — два окна по 6 ч, `2024-05-10T13:00Z..19:00Z` и
  `2024-05-11T00:00Z..06:00Z`, внутри выраженного события AR3664 (§3,
  «2024-05-10…11»: протонное событие S1, буря Kp до 9). `as_of =
  2024-05-11T04:30Z` выбран сразу после последнего уведомления SEP,
  нужного второму окну (`20240511-AL-008`, опубликовано
  `2024-05-11T04:13:59Z`) — «безопасно после», не намеренно позднее
  необходимого. Орбита — выпуск OEM `2024-05-08` (`USEABLE_START_TIME
  05-08T12:00Z .. USEABLE_STOP_TIME 05-23T12:00Z`, опубликован
  `2024-05-08T20:28:59Z`), полностью покрывает оба окна и опубликован
  задолго до `as_of`.
- **control** — `2024-06-20T06:00Z..2024-06-21T12:00Z`, внутри
  контрольного спокойного периода §3 (16–27 июня: нет уведомлений
  SEP/GST). Намеренно 20–21 июня, а не 16-е число само по себе, чтобы
  однозначно остаться и вне пробела архива SWPC Forecast Discussion
  (15.05–16.06.2024 — эта линия здесь не используется, но граница пробела
  всё равно не задета), и вне бури 28 июня. Орбита — выпуск OEM
  `2024-06-18` (`USEABLE_START_TIME 06-17T12:00Z .. USEABLE_STOP_TIME
  07-02T12:00Z`).
- **missing_data** — `2024-05-28T00:00Z..2024-05-29T00:00Z`, строго между
  `USEABLE_STOP_TIME` выпуска `2024-05-12` (`2024-05-25T12:00Z`) и
  `USEABLE_START_TIME` выпуска `2024-06-14` (`2024-06-14T12:00Z`) —
  реальный, задокументированный пробел архива OEM
  (`tests/fixtures/orbit/history/README.md`), а не сценарий, специально
  подстроенный под отказ: `experiments.production.build_production_result`
  вызывает ровно тот же `src.sources.orbit.select_oem_elements_for_request`,
  что и обслуживал бы боевой `historical_forecast`, и получает тот же
  `HistoricalElementsUnsupportedError`, что получил бы он
  (`tests/experiments/test_missing_data.py` дополнительно перепроверяет
  сам факт пробела по всем четырём реальным выпускам).

### 10.2 Что такое базовый метод здесь и чем он НЕ является

[`experiments/baseline.py`](../experiments/baseline.py) реализует
main-prompt.md §11 буквально: единственное DONKI-уведомление типа SEP/GST
(та же пара, что main-prompt.md §11 называет для контрольного периода) с
известным временем публикации, самое позднее до `as_of` — без ограничения
по давности («перенос на весь горизонт»), — переносится на ОБА окна
одинаково, без проверки пересечения с самим окном. Три возможных исхода:
`no_prior_observation` (архив до `as_of` вообще пуст — не путается со
«спокойно»), `event_carried_forward`, `quiet_carried_forward` (последнее
уведомление — не SEP/GST, например `Report`/`CME`/`FLR`). Базовый метод
структурно не может предпочесть одно окно другому — обоим окнам всегда
один и тот же вердикт — это не недоработка, а прямое следствие «без
расчёта пересечений».

Базовый метод НЕ является ни production-классификацией
(`experiments/production.py`), ни archival evidence-пробой
(`experiments/donki_evidence.py`, не шкала S NOAA) — оба явно отдельные
модули; `tests/experiments/test_baseline_independence.py` проверяет это не
соглашением, а структурно (`ast`-анализ импортов `experiments/baseline.py`).

### 10.3 Честный вывод — то, что реально показал прогон

Дословно по `experiments/out/summary.md` после `uv run python -m experiments.run`:

- `mechanisms[*space_weather].status = "missing_data"` на КАЖДОМ окне
  обоих сценариев с production-результатом (`event`, `control`) — в
  репозитории нет архивного количественного наблюдения потока протонов
  (только живой `noaa-swpc-proton-flux`, никогда не `replay_eligible`);
  строгая DONKI→шкала-S классификация — предмет FN-41/FN-42 (не слиты).
  Прямое следствие правила критического пробела (main-prompt.md §11, п.1):
  `recommendation.status = "all_windows_excluded"` для ОБОИХ сценариев —
  не баг этой сессии, а честный, воспроизводимый исход.
- `mechanisms[*mmod].status = "ok"` в обоих сценариях (средний
  `coverage_fraction = 1.0` по обоим окнам) — MMOD не «спасает»
  рекомендацию: правило критического пробела действует по меньшему из
  двух механизмов, не по среднему.
- Архивная evidence-проба DONKI (не шкала S) нашла реальные SEP/GST-
  предупреждения, пересекающиеся с ОБОИМИ окнами сценария `event`
  (`donki_evidence_window_fraction = 1.0`), и ни одного — для сценария
  `control` (`= 0.0`) — согласуется с таблицей §3.
- Базовый метод на этом конкретном прогоне тоже дал `event_carried_forward`
  для `event` (basis — `20240511-AL-008`, SEP) и `quiet_carried_forward`
  для `control` (basis — последнее известное к `as_of` уведомление,
  `20240620-AL-002`, тип `FLR`, не SEP/GST) — `event_miss`/`false_warning`
  = `False` по обеим сторонам на этом прогоне. Совпадение направления не
  означает эквивалентность методов: evidence-проба проверяет пересечение с
  КАЖДЫМ окном отдельно, базовый метод — нет (см. §10.2); метрика
  `selected_window_change` фиксирует, что ни один метод не выражает
  предпочтения одного окна другому на этом прогоне, но по разным честным
  причинам — production явно отказывается (критический пробел
  `space_weather`), базовый метод структурно не умеет сравнивать окна
  вовсе.
- `missing_data`: `production_failure.json` содержит
  `HistoricalElementsUnsupportedError` — ни один выпуск OEM не покрывает
  запрошенный интервал целиком, будучи опубликованным (S3 `LastModified`)
  к `as_of`. Честный отказ расчёта, не подменённый спокойным результатом.

**Строгий вывод сессии.** Этот стенд не формулирует и не может
формулировать вывод о допуске к ВКД (main-prompt.md §11 «сервис никогда не
формулирует вывод как допуск») — только сравнение двух методов
интерпретации архивных данных на трёх заранее обоснованных датах, с
машиночитаемой трассировкой к использованным записям в каждом
`experiments/out/<scenario>/*.json`.
