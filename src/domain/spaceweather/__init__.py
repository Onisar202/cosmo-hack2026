"""Механизм 1: интерпретация космической погоды, уровни, прогноз.

``external_forecast.py`` (FN-31) — внешний суточный прогноз-вероятность
NOAA 3-Day; ``observed_classifier.py`` (FN-38) — классификация НАБЛЮДЕНИЯ
(поток протонов GOES) по шкале S NOAA; ``historical_events.py`` (FN-41) —
архивная СОБЫТИЙНАЯ линия (уведомления DONKI) для исторических окон, у
которой нет ни измеренной величины, ни вероятности, только три состояния
EVENT_PRESENT/NO_EVENT_DETECTED/INSUFFICIENT_DATA. Разные величины, ни один
из модулей не импортирует другой — комбинирует их только
``src/api/service.py``.
"""
