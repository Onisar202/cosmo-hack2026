"""Слой расчётов: чистые функции без сети и обращений к хранилищу.

Подпакеты изолированы друг от друга и не импортируются напрямую между
собой — общее передаётся явно через результат geometрии из ``orbit/``
(см. .ai/main-prompt.md, §8, §11).
"""
