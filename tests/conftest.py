"""Общий env для тестов: без сети и без локального .env.

Значение выставляется на импорте conftest.py — раньше, чем тестовые
модули успеют импортировать ``src.api.app`` (и тем самым построить
``Settings``). Так тесты не зависят от наличия локального .env.
"""

import os

os.environ.setdefault("APP_ENV", "development")
